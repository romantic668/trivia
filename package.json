{
  "name": "awesome-game",
  "version": "1.0.0",
  "description": "The web server will serve files as static from the static folder the web-server shall proxy to the app running out of the app folder.",
  "main": "index.js",
  "scripts": {
    "start": "cd app && npm install && node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "6.9.5"
  },
  "keywords": [],
  "author": "Lin Cong",
  "license": "ISC"
}